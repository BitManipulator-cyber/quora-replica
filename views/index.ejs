<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>First RESTful API</title>
</head>
<link rel="stylesheet" href="/style.css">
<body>
    <h1>Quora Posts</h1>
    <% for (post of posts) { %>
        <div>
            <h3>@<%= post.username %></h3>
            <p><%= post.content %></p>
            <a href="http://localhost:8080/posts/<%= post.id %>">Look at post in detail</a>
            <br>
            <a href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a>
            <br>
            <br>
            <form method="post" action="/posts/<%= post.id %>?_method=DELETE">
                <button>Delete Post</button>
            </form>
        </div>    
    <% } %>

    <br>
    <a href="http://localhost:8080/posts/new">Create a New post</a>
</body>
</html>